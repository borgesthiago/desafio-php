{% extends 'base.html.twig' %}

{% block title %}Funcionario{% endblock %}

{% block body %}
    <h1>Funcionario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ funcionario.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ funcionario.nome }}</td>
            </tr>
            <tr>
                <th>Matricula</th>
                <td>{{ funcionario.matricula }}</td>
            </tr>
            <tr>
                <th>CPF</th>
                <td>{{ funcionario.cpf }}</td>
            </tr>
            <tr>
                <th>Endereço</th>
                <td>{{ funcionario.endereco }}</td>
            </tr>
            <tr>
                <th>Secretaria</th>
                <td>{{ funcionario.secretaria.nome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('funcionario_index') }}"><button type="button" class="btn btn-sm btn-secondary"><i class="fa fa-arrow-circle-left"></i> Voltar</button></a>

    <a href="{{ path('funcionario_editar', {'id': funcionario.id}) }}"><button type="button" class="btn btn-sm btn-warning"><i class="fa fa-edit"></i> Editar</button></a>

    {{ include('funcionario/_delete_form.html.twig') }}
{% endblock %}
